"use client";

/**
 * Tenant branding colors are used only on login/settings preview —
 * main app UI keeps the green-blue-violet gradient system.
 */
export function BrandingTheme() {
  return null;
}
